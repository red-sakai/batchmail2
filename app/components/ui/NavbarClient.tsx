"use client";

// Tabs removed per request; navbar no longer controls view switching.

export default function NavbarClient() {
  return (
    <div className="text-sm opacity-80">
      {/* Placeholder: Could add settings/help links later */}
    </div>
  );
}
